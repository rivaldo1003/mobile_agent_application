import 'package:flutter/material.dart';

class ConfigMode extends StatefulWidget {
  const ConfigMode({Key? key}) : super(key: key);

  @override
  State<ConfigMode> createState() => _ConfigModeState();
}

class _ConfigModeState extends State<ConfigMode> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
